"""Fixture scene setter — structured NRL match context + weather.

Facts only — no LLM. Call this before qualitative research and the math engine.
"""

TOOL_NAME = "fixture_scene"
TOOL_VERSION = "0.2.0"
